<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:LabSampleManager.Desktop.Controls"
    xmlns:resources="clr-namespace:LabSampleManager.Desktop.Properties">
    <Style TargetType="{x:Type local:DateTimePickerControl}">
        <Setter
            Property="AutomationProperties.Name"
            Value="{Binding Title, RelativeSource={RelativeSource Self}}"/>
        <Setter
            Property="Foreground"
            Value="{DynamicResource Field.Text}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DateTimePickerControl}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Text="{TemplateBinding Title}"
                                FontWeight="SemiBold"/>
                            <TextBlock
                                x:Name="PART_Indicator"
                                Grid.Column="1"
                                Text="{x:Static resources:Resources.RequiredFieldIndicator}"
                                Foreground="{DynamicResource Field.Error}"
                                Margin="4,0,0,0"
                                Visibility="Collapsed"/>

                            <Grid
                                Grid.ColumnSpan="2"
                                Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <DatePicker
                                    x:Name="PART_DatePicker"
                                    Grid.Column="0"/>
                                <TextBox
                                    x:Name="PART_TimeTextBox"
                                    Grid.Column="2"
                                    Padding="10,8"/>
                            </Grid>
                        </Grid>

                        <TextBlock
                            Text="{TemplateBinding SubTitle}"
                            Margin="0,4,0,0"
                            Foreground="{DynamicResource Field.Muted}"
                            FontSize="12"
                            TextWrapping="Wrap"/>
                    </StackPanel>

                    <ControlTemplate.Triggers>
                        <Trigger
                            Property="ShowFieldIndicator"
                            Value="True">
                            <Setter
                                TargetName="PART_Indicator"
                                Property="Visibility"
                                Value="Visible"/>
                        </Trigger>

                        <Trigger
                            Property="IsEnabled"
                            Value="False">
                            <Setter
                                TargetName="PART_Indicator"
                                Property="Visibility"
                                Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
