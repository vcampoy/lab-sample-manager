<UserControl x:Class="LabSampleManager.Desktop.Controls.HeaderControl"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100"
             d:DesignWidth="800"
             Height="100"
             HorizontalContentAlignment="Stretch"
             VerticalContentAlignment="Center">
    <Grid Margin="30,18">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Image x:Name="HeaderIcon"
               Grid.Column="0"
               Width="28"
               Height="28"
               Margin="0,0,12,0"
               Source="pack://application:,,,/LabSampleManager.Desktop;component/Assets/Icons/Fluent/Beaker/ic_fluent_beaker_24_filled.png"
               Stretch="Uniform"
               ToolTip="icon-beaker-header"
               AutomationProperties.Name="icon-beaker-header" />

        <StackPanel Grid.Column="1"
                    Margin="0,0,28,0"
                    VerticalAlignment="Center">
            <TextBlock Text="{Binding Title, ElementName=Root}"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#142033"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="NoWrap" />
            <TextBlock Text="{Binding SubTitle, ElementName=Root}"
                       Margin="0,2,0,0"
                       FontSize="14"
                       Foreground="#66758C"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="NoWrap" />
        </StackPanel>

        <StackPanel Grid.Column="2"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <StackPanel Margin="0,0,16,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">
                <TextBlock Text="Dr. Bruce Banner"
                           HorizontalAlignment="Right"
                           FontSize="14"
                           Foreground="#243B5A"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap" />
                <TextBlock Text="Laboratory Scientist"
                           Margin="0,2,0,0"
                           HorizontalAlignment="Right"
                           FontSize="12"
                           Foreground="#66758C"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap" />
            </StackPanel>

            <Border Width="46"
                    Height="46"
                    Background="#DCEBFA"
                    CornerRadius="23">
                <TextBlock Text="BB"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#24578A" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
